// Math
//Standard built-in objects which are always available

const number = 5.98221

// Rounding numbers down

const roundDown = Math.floor(number)

// Rounding numbers up

const roundUp = Math.ceil(number)



// Square roots 

const number2 = 81
const squaredNumber= Math.sqrt(number2)


// Find the PI value  

const PIValue= Math.PI

// Find the min value 

const minValue =Math.min(8, 7, 2, 0.3, 5, 1, 6)

// Find the max value

const maxValue = Math.max(299, 100, 399, 823, 502, 39, 177)



// Return random values
// Returns random numbers from 0 to 0.99999.... and 1 is not included

const randomNumber = Math.random()

console.log(randomNumber)